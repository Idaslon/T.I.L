# Migration `20200730011449-create-user-video-progress`

This migration has been generated by DestroyeerU at 7/30/2020, 1:14:49 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."UserVideoProgress" (
"id" SERIAL,
"value" Decimal(65,30)  NOT NULL ,
"videoId" integer  NOT NULL ,
"userId" integer  NOT NULL ,
PRIMARY KEY ("id"))

ALTER TABLE "public"."UserVideoProgress" ADD FOREIGN KEY ("videoId")REFERENCES "public"."Video"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."UserVideoProgress" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200730011420-create-video..20200730011449-create-user-video-progress
--- datamodel.dml
+++ datamodel.dml
@@ -2,21 +2,34 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id    Int    @default(autoincrement()) @id
-  name  String
-  email String @unique
+  id                Int                 @default(autoincrement()) @id
+  name              String
+  email             String              @unique
+  UserVideoProgress UserVideoProgress[]
 }
 model Video {
-  id   Int    @default(autoincrement()) @id
-  path String @unique
+  id                Int                 @default(autoincrement()) @id
+  path              String              @unique
+  UserVideoProgress UserVideoProgress[]
 }
+
+model UserVideoProgress {
+  id    Int   @default(autoincrement()) @id
+  value Float
+
+  video   Video @relation(fields: [videoId], references: [id])
+  videoId Int
+
+  user   User @relation(fields: [userId], references: [id])
+  userId Int
+}
```


